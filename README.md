# FENR web

Static marketing site for FENR.

```sh
npm install
npm run dev
```

Create the production build with `npm run build`.

The temporary TestFlight URL is defined once in `app/site-config.ts`.
